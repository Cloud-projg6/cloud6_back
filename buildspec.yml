version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto17
  build:
    commands:
      - cd book-server
      - mvn -B package -DskipTests
artifacts:
  base-directory: book-server
  files:
    - target/*.jar
    - appspec.yml
    - scripts/**/*